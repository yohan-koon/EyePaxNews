import styled from 'styled-components/native';
import {BarButton} from '../../../components/bar-button/bar-button.component';

export const NewsCategoryWrapper = styled.View`
  flex-direction: row;
`;

export const FilterButton = styled(BarButton)`
  
`;
